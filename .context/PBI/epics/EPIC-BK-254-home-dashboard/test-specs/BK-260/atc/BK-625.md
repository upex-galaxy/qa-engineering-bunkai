# ATC Spec: BK-625 — navigateToActivityFromHeaderLink

> Ticket: BK-260
> Component: HomePage (tests/components/ui/HomePage.ts)
> Type: UI — Navigation
> Parent Story: BK-260

## 1. Test Case Summary

| Name | Objective | Precondition | Acceptance Criteria |
|---|---|---|---|
| navigateToActivityFromHeaderLink | Confirm the header "View all" link takes the member to `/activity` | Home's recent-activity card header is rendered (true in every feed state — list, empty, error, skeleton resolved) | AC2 |

## 2. ATC Contract

```typescript
/**
 * ATC: Click the recent-activity card's header "View all" link and verify
 * navigation to the full activity view.
 * Fixed assertions:
 *  - Page URL becomes /activity
 */
@atc('BK-625')
async navigateToActivityFromHeaderLink(): Promise<void> { /* ... */ }
```

## 3B. UI Details

- Page path: `/home` → `/activity`
- Locators: `recentActivityViewAllHeader()` (`home-recent-activity-view-all`)
- Playwright assertions: `await expect(this.page).toHaveURL(/\/activity$/)`

## 4. Assertions Split

### Fixed (inside ATC)
- Navigated to `/activity` after the click

### Test-level (in test file)
- None

## 5. Code Template

```typescript
@atc('BK-625')
async navigateToActivityFromHeaderLink(): Promise<void> {
  await this.goto();
  await this.recentActivityViewAllHeader().click();
  await this.page.waitForURL(/\/activity$/);
  await expect(this.page).toHaveURL(/\/activity$/);
}
```

Test file usage:

```typescript
test('BK-260: should navigate to /activity when the header View all link is selected', async ({ ui }) => {
  await ui.home.navigateToActivityFromHeaderLink();
});
```

## 6. Technique-derivation check

| AC | Technique fired | ATCs produced |
|---|---|---|
| AC2 (View all navigates to full activity view) | EP (happy-path navigation, always-rendered element) | BK-625 — one ATC, header link only |

**Equivalence Partitioning detail:**

| Input | Expected output | Same ATC? |
|---|---|---|
| Click header "View all" link | Navigate to `/activity` | N/A — single representative case, no invalid partition exists for a link click (BVA/State-Transition/Decision-Table: all N/A, see below) |

**Boundary Value Analysis**: N/A — no range, limit, length, or date window involved.
**State-Transition**: N/A — the header link is not gated by any entity state (renders identically whether the feed has rows, is empty, or errored — confirmed by reading `RecentActivityShell` in `RecentActivity.tsx`, which wraps every state).
**Decision Table**: N/A — single, unconditional trigger.
**Pairwise**: N/A — <3 factors.

**Two reduction axes**: N/A — not a parameterized ATC.

## 7. Dependencies

- Precondition Steps: none
- Required Components: none beyond `HomePage` itself — no API setup needed (see automation-plan.md §2 "Fixture challenge")

## 8. Data Context

No precondition data required — the header link renders in every feed state, so this ATC runs against whatever workspace is already active for the test user (default landing state). Cleanup: none.

## 9. Checklist

- [x] `verb{Resource}{Scenario}` naming — `navigateToActivityFromHeaderLink`
- [x] Max 2 positional params — 0 params
- [x] Correct return type — `void` for UI ATC
- [x] Fixed vs test-level assertions split (§4)
- [x] Not duplicating an existing ATC — confirmed against `kata-manifest.json`; also distinct from BK-626 (different DOM location / locator, per the Story's own Test issue wording "distinct DOM location from TC2")
