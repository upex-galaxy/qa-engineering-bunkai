# ATC Spec: BK-626 — navigateToActivityFromEmptyState

> Ticket: BK-260
> Component: HomePage (tests/components/ui/HomePage.ts)
> Type: UI — Navigation
> Parent Story: BK-260

## 1. Test Case Summary

| Name | Objective | Precondition | Acceptance Criteria |
|---|---|---|---|
| navigateToActivityFromEmptyState | Confirm the empty-state's own "Browse the full activity feed" link takes the member to `/activity` | Workspace has zero tracked activity in the last 24h (empty state renders) | AC2 (empty-state variant, distinct DOM location from BK-625) |

## 2. ATC Contract

```typescript
/**
 * ATC: Given the feed is showing its empty state, click the empty state's own
 * "Browse the full activity feed" link and verify navigation to the full
 * activity view.
 * Fixed assertions:
 *  - Page URL becomes /activity
 */
@atc('BK-626')
async navigateToActivityFromEmptyState(): Promise<void> { /* ... */ }
```

## 3B. UI Details

- Page path: `/home` → `/activity`
- Locators: `recentActivityEmptyViewAll()` (`home-recent-activity-empty-view-all`) — distinct testid from BK-625's header link, confirming this is genuinely a different DOM node, not a duplicate assertion of the same element
- Playwright assertions: `await expect(this.page).toHaveURL(/\/activity$/)`

## 4. Assertions Split

### Fixed (inside ATC)
- Navigated to `/activity` after the click

### Test-level (in test file)
- None

## 5. Code Template

```typescript
@atc('BK-626')
async navigateToActivityFromEmptyState(): Promise<void> {
  await this.goto();
  await this.recentActivityEmptyViewAll().click();
  await this.page.waitForURL(/\/activity$/);
  await expect(this.page).toHaveURL(/\/activity$/);
}
```

Test file usage:

```typescript
test.beforeAll(async ({ api }) => {
  const [, ws] = await api.home.createWorkspace({ name: 'BK-260 empty link', slug: dataFactory.generateWorkspaceSlug() });
  workspaceId = ws.workspace.id;
});

test('BK-260: should navigate to /activity when the empty-state link is selected', async ({ test: fixture }) => {
  await fixture.ui.home.useWorkspace(workspaceId);
  await fixture.ui.home.navigateToActivityFromEmptyState();
});
```

## 6. Technique-derivation check

| AC | Technique fired | ATCs produced |
|---|---|---|
| AC2 (empty-state variant of View all navigation) | EP (happy-path navigation, distinct DOM location from BK-625) | BK-626 — one ATC |

**Equivalence Partitioning detail:**

| Input | Expected output | Same ATC? |
|---|---|---|
| Click empty-state "Browse the full activity feed" link | Navigate to `/activity` | N/A — single representative case. NOT merged with BK-625 despite the identical destination and identical underlying business rule (AC2): the Story's own Test issue (BK-626) explicitly calls this "empty-state variant of the link, distinct DOM location from TC2" — kata-architecture.md Rule 3 only permits merging same-output cases WITHIN a partition, and "which DOM node fires the navigation" is itself a distinct precondition (feed state: rows vs. empty) per the TC-identity rule (Rule 2: precondition changed → distinct TC). |

**Boundary Value Analysis**: N/A. **State-Transition**: the PRECONDITION is a state (empty feed) but the transition itself is not under test here (BK-627 owns "does the empty state render correctly"; this ATC only owns "does ITS link navigate correctly", given the state already holds) — N/A for this ATC specifically. **Decision Table**: N/A. **Pairwise**: N/A.

**Two reduction axes**: N/A — not a parameterized ATC.

## 7. Dependencies

- Precondition Steps: none
- Required Components: `HomeApi.createWorkspace` (new, this ticket) for the empty-workspace precondition

## 8. Data Context

See automation-plan.md §4, row "BK-626" — Generate (throw-away workspace via `HomeApi.createWorkspace`), matching HD-T01/BK-850's precedent. Cleanup: none.

## 9. Checklist

- [x] `verb{Resource}{Scenario}` naming — `navigateToActivityFromEmptyState`
- [x] Max 2 positional params — 0 params
- [x] Correct return type — `void` for UI ATC
- [x] Fixed vs test-level assertions split (§4)
- [x] Not duplicating an existing ATC — confirmed against `kata-manifest.json`; distinct from BK-625 (different testid/DOM location) and from BK-627 (different assertion: link-click-navigates vs. empty-state-renders-correctly)
