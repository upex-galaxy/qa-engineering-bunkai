# ATC Spec: BK-627 — viewRecentActivityEmptyState

> Ticket: BK-260
> Component: HomePage (tests/components/ui/HomePage.ts)
> Type: UI — Verification
> Parent Story: BK-260

## 1. Test Case Summary

| Name | Objective | Precondition | Acceptance Criteria |
|---|---|---|---|
| viewRecentActivityEmptyState | Confirm the empty state renders with its scoped copy and offers the browse link, when the list does not | Workspace has zero tracked activity in the last 24h | AC3 |

## 2. ATC Contract

```typescript
/**
 * ATC: Navigate to Home and verify the recent-activity empty state renders
 * with its scoped copy, and the item list does not render.
 * Fixed assertions:
 *  - Empty state (home-recent-activity-empty) is visible
 *  - Empty state copy names only what THIS feed tracks (not a whole-workspace claim)
 *  - Empty-state "Browse the full activity feed" link is visible
 *  - Item list (home-recent-activity-list) is NOT rendered
 */
@atc('BK-627')
async viewRecentActivityEmptyState(): Promise<void> { /* ... */ }
```

## 3B. UI Details

- Page path: `/home`
- Locators: `recentActivityEmpty()` (`home-recent-activity-empty`), `recentActivityList()` (`home-recent-activity-list`), `recentActivityEmptyViewAll()` (`home-recent-activity-empty-view-all`, shared with BK-626 — extract to `private readonly`, used in 2+ ATCs)
- Playwright assertions: `toBeVisible()`, `toContainText()` for the scoped copy, `not.toBeVisible()` / `toHaveCount(0)` for the list

## 4. Assertions Split

### Fixed (inside ATC)
- Empty state visible, list absent, scoped copy present, browse link present — all one TC's assertions per the TC-identity rule (same precondition, same action, multiple expected results)

### Test-level (in test file)
- None

## 5. Code Template

```typescript
@atc('BK-627')
async viewRecentActivityEmptyState(): Promise<void> {
  await this.goto();
  await expect(this.recentActivityEmpty()).toBeVisible();
  await expect(this.recentActivityEmpty()).toContainText('No tracked events in the last 24 hours');
  await expect(this.recentActivityEmpty()).toContainText('new modules, ATCs and tests, finished runs and defect');
  await expect(this.recentActivityEmptyViewAll()).toBeVisible();
  await expect(this.recentActivityList()).toHaveCount(0);
}
```

Test file usage:

```typescript
test.beforeAll(async ({ api }) => {
  const [, ws] = await api.home.createWorkspace({ name: 'BK-260 empty state', slug: dataFactory.generateWorkspaceSlug() });
  workspaceId = ws.workspace.id;
});

test('BK-260: should show an empty state when the workspace has no tracked activity in the last 24 hours', async ({ test: fixture }) => {
  await fixture.ui.home.useWorkspace(workspaceId);
  await fixture.ui.home.viewRecentActivityEmptyState();
});
```

## 6. Technique-derivation check

| AC | Technique fired | ATCs produced |
|---|---|---|
| AC3 (empty state when no recent activity) | EP (single valid partition: zero tracked events) | BK-627 — one ATC |

**Equivalence Partitioning detail:**

| Input | Expected output | Same ATC? |
|---|---|---|
| Workspace with zero tracked events in 24h | Empty state renders, scoped copy, browse link, no list | N/A — single representative case. The COMPLEMENT partition (workspace WITH tracked events → list renders, not empty state) is covered by BK-624, a distinct ATC (different precondition, different expected output — not a variant of this one). |

**Boundary Value Analysis**: the "zero events" precondition is itself the boundary-adjacent case of BK-624's "≥1 event" precondition (0 vs. 1+ is the natural EP/BVA pair for a count-based condition) — already captured as two SEPARATE ATCs (BK-627 vs. BK-624) per the split-rule in test-design-doctrine.md Part 2.5 ("different behavior = different artifact, even if the input field is the same"). No further boundary exists within "zero" itself. **State-Transition**: N/A — not a stateful entity transition, a point-in-time render check. **Decision Table**: N/A — single condition. **Pairwise**: N/A.

**Two reduction axes**: N/A — not a parameterized ATC.

## 7. Dependencies

- Precondition Steps: none
- Required Components: `HomeApi.createWorkspace` (new, this ticket)

## 8. Data Context

See automation-plan.md §4, row "BK-627" — Generate (throw-away workspace via `HomeApi.createWorkspace`), matching HD-T01/BK-850's precedent exactly (same empty-workspace mechanism, different widget). Cleanup: none.

## 9. Checklist

- [x] `verb{Resource}{Scenario}` naming — `viewRecentActivityEmptyState`
- [x] Max 2 positional params — 0 params
- [x] Correct return type — `void` for UI ATC
- [x] Fixed vs test-level assertions split (§4)
- [x] Not duplicating an existing ATC — confirmed against `kata-manifest.json`; distinct from BK-624 (opposite precondition/output) and BK-626 (renders-correctly vs. link-navigates)
